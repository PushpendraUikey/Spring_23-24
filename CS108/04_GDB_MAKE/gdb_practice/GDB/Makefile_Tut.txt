1)  @, which tells Make not to display the command itself. 

2)  If you run: make 
          this will look for a file named makefile in current directory and then execute
          it. If you have several makefiles, then you can execute them with the command
          make -f MyMakefile 

3)  Makefile are used to help decide which parts of a large program need to be recompiled.
    The goal of Makefiles is to compile whatever files need to be compiled, based
    on what files have changed.